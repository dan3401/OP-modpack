giga_contact_aeternum = {
    potential = {
        exists = from
        OR = {
            any_country = {
                is_country_type = dormant_aeternum
            }
            has_global_flag = aeternum_vanquished
        }
        has_country_flag = aeternite_agency_unlocked
    }
    allow = {
        hidden_trigger = {
            NOT = {
                has_country_flag = monitoring_aeternum
            }
        }
    }
    effect = {
        custom_tooltip = "giga_contact_aeternum_tooltip"
        hidden_effect = {
            country_event = {
                id = giga_aeternum.3010
            }
        }
    }
}
